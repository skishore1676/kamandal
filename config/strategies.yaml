# DEPRECATED — strategy definitions have moved to a two-file architecture:
#
#   config/strategy_templates.yaml  → structure-level templates (how to build)
#   config/underlying_profiles.yaml → universe groupings (where to apply, how much)
#
# At runtime, the resolver merges them: template + profile → Strategy.
# This file is kept for backward compatibility with the backtester's
# `backtest_approved` flag writer, which updates strategies by id.
# It will be removed once the backtester is updated.

strategies: []
